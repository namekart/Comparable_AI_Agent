# version: '3.8'

# services:
#   domain-api:
#     build: .
#     container_name: domain-comparable-agent
#     ports:
#       - "8000:8000"
#     environment:
#       - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
#       - PYTHONUNBUFFERED=1
#     restart: unless-stopped
#     healthcheck:
#       test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
#       interval: 30s
#       timeout: 10s
#       retries: 3
#       start_period: 60s